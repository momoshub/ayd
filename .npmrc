# deterministic, strict installs — open-source-quality hygiene
engine-strict=true
save-exact=false
# electron needs to run its postinstall; allow only vetted build scripts
# (pnpm 10+ blocks lifecycle scripts by default; approved list below)
